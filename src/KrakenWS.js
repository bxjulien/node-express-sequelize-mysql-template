import { KrakenWSPublic } from 'node-kraken-ws'
const axios = require('axios');


/* async () => {
  const public = new KrakenWSPublic()
  await public.connect()

  const {
    ...originalPayload,
    unsubscribe, // Function
  } = await instance.subscribe({"event": "ping"})
} */


